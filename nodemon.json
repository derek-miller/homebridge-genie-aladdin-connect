{
  "watch": ["src"],
  "ext": "ts",
  "ignore": [],
  "exec": "tsc && node --inspect ./node_modules/.bin/homebridge -I -D --user-storage-path ./config",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}
